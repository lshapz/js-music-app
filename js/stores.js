let stores = []
//let stores = [{artists: [], songs:[], videos:[]}]

function store(){
  return stores[stores.length-1]
}

var albumStore = []

function albumStore(){
  return albumStore[albumStore.length-1]
}
